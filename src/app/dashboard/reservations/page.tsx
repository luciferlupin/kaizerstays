import ReservationsClient from "./ReservationsClient";

export const metadata = {
  title: "Reservations — KaizerStays",
};

export default function ReservationsPage() {
  return <ReservationsClient />;
}
