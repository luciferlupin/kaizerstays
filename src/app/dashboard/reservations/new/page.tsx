import CreateReservationClient from "./CreateReservationClient";

export const metadata = {
  title: "Create Reservation — KaizerStays",
};

export default function CreateReservationPage() {
  return <CreateReservationClient />;
}
