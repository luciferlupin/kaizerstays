import ReservationDetailClient from "./ReservationDetailClient";

export const metadata = {
  title: "Reservation Detail — KaizerStays",
};

export default function ReservationDetailPage() {
  return <ReservationDetailClient />;
}
