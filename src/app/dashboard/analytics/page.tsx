import AnalyticsClient from "./AnalyticsClient";

export const metadata = {
  title: "Analytics & Revenue KPIs — KaizerStay",
};

export default function AnalyticsPage() {
  return <AnalyticsClient />;
}
