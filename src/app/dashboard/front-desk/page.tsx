import FrontDeskClient from "./FrontDeskClient";

export const metadata = {
  title: "Front Desk Workspace — KaizerStay",
};

export default function FrontDeskPage() {
  return <FrontDeskClient />;
}
